# 2026-09-01

- 小三月独立评审 FR-019 锚点保底注入（设计文档 / 插件 uncommitted diff / eval_td_regression.py 锚点推演段 / hybrid+nosummary 两份输出 + queries.yaml v3 + 新快照），只读评审。
- 关键结论：实现与设计伪码有 4 处偏差（rank_map 解包 bug、fused 外排序口径不一、text_for None 检查、events_for_date str 分支未登记）；评测 harness 与生产不一致（无 hybrid_keep=10 截断、时间源用「提及于」正则 vs 生产 created_at）；P0 = 评测运行实际用 284 事件新快照但脚本/queries.yaml 仍指向 197 事件旧快照（sha1 7651ebec），可复现性断裂；「0 触发」在 harness 口径成立但 anchor 场景 4 条查询无一真正命中机制，验收门空转；temporal 回归（0.667→0.583）两臂一致，归因语料扩容（197→284）而非 FR-019。
- 证据要点：#37 gold（2850094f 等 8/31 事件）不在旧快照但 mrr=1.0 → 运行用新快照；设计预测受益查询 #5/#12 在新语料下自愈（mrr/rec=1.0）；生产 rows 为空时锚点被 early-return 短路（__init__.py L803-806）。
- 完整评审报告落盘：`.workbuddy/review-fr019-anchor-backfill-20260901.md`（Summary + P0×1 / P1×5 / P2×6 + Unverified×5）。
