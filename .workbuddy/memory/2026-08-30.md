# 2026-08-30

- 小三月独立评审概要层（summary layer）T-D 回归失败（只读，未改代码）。核心发现：temporal 0.667→0.194 并非单一「时间词直通负贡献」，而是四种根因叠加：
  1. eval_td_regression.py 直通日期**升序**取窗口内最旧 2 天（8/24-25），插件生产代码**降序**取最新 2 天（8/29-30）→ #17/#22 被误判，生产实际大概率能命中 gold。
  2. 350 字符预算按时间正序截断日概要：8/30 有 30 事件，约第 2 行即截断，#21 的 gold 位于第 2/6/8/21/23 行 → 直通日期正确但内容全被截掉。
  3. 「上周/昨天」标注锚点为周一 8/31 视角（上周=8/24-29、昨天=8/30），EVAL_NOW 冻在周日 8/30 17:24 → #19/#20 窗口错一周、#18 gold 出窗一天。属标注 deixis 不一致，非解析器缺陷。
  4. 附加混淆：v3 基线事件段预算 900 vs T-D 850；T-D harness 对照跑 0.742<0.769 亦含 vector 腿 query 双重 JSON 编码嫌疑；rerank 4s 超时 fail-open 造成 0.618-0.714 波动。
- 建议：方案 D（先修度量：deixis 校验器 + 直通排序对齐 + Recall@context 双指标；产品侧用「直通降级软信号」替代方案 A 的 6 天硬 cutoff；「上周」改 ISO 日历周解析 + ±2 天 padding；单日窗 ±1 天 padding 吸收 flush 漂移；中期启用 kind=weekly 概要）。
- 评审依据文件：specs/001-summary-layer/spec.md、~/.hermes/plugins/memobase/{__init__,hybrid_retriever}.py、memu-research/eval-out/{queries.yaml,eval_td_regression.py,td-regression-*.json,eval-results-final.json,events-snapshot.json}。
- 复审 agy 的修复报告（`specs/001-summary-layer/td-fix-report-20260830.md`）：D1/D2 落实、数字可复现，但「T-D 通过」不成立——(a) 新度量 `mixed=event_events` 两臂同源，MRR 差异只能是 rerank 噪声（36 条仅 #33 不同，top3 第三位两跑不同）；(b) 概要段对 Recall 贡献 36/36 = 0（#33 gold 全在 8/25-29，概要拉 8/27+8/30 无 gold）；(c) 脚本 L342 仍执行 0.85 旧闸门自报未通过，闸门变更只在报告层；(d) #21 两臂 0.0（截断未修）；(e) 原失败运行 JSON 被原地覆盖，审计链断裂。价值漏斗：选日 0.50（temporal 0.78）→ 行级存活 0 → 增益 0。复审报告落档 `specs/001-summary-layer/re-review-td-fix-20260830.md`，建议暂不开 flag，先修 350 行级截断 + rerank 状态入 JSON + SC-001 三方一致修订。
- 评审报告落档：`specs/001-summary-layer/review-td-regression-20260830.md`（含根因分解表、方案 D 三步走、直通降级软信号设计、附录证据对照）。
- 语料偏斜备忘：197 事件中 84% 集中在 8/27-8/30（166 条），8/14-8/18 空洞；R2 doc 形态 0.894 优势部分是密度伪影，随历史增长 topk=2 对多天窗覆盖率会衰减。
