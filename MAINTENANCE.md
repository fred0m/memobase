# Memobase 记忆维护日志

维护节奏：**每月一次体检**（手动为主 + 低频提醒）。
体检项：画像层空壳/重复/错记 → 事件层噪音 → 孤儿用户。

自动化（不需要手动）：
- 事件层：每轮对话自动写 buffer，满 1024 token 自动 LLM 提取
- 概要层：每天自动 LLM 蒸馏主题化日概要（style=llm，2026-08-31 修复）
- 召回层：BM25 + 向量 + 实体三腿 RRF，每轮自动注入

手动的只有两件：**画像纠错**（事件层更正不会自动同步到画像快照）、**偶发空壳清理**。

---

## 2026-09-17（文件层整理：纠错为主，非删除）

**背景**：沫沫问「记忆整理该手动还是自动」→ 实测体检发现文件层（MEMORY/USER）积了硬错误，Memobase 侧反而很干净。

**Memobase 实测（本次未动数据）**：931 event / 3653 gist / 35 概要 / 2 user。
近 7 天新增 310 event / 1390 gist（~44 event/天）。短 gist(<15字) 仅 4 条、空占位 0 条 → **垃圾率 0.1%，不需要手动整理**。
概要层连续 14 天正常生成 ✅。结论：自动层健康，手动只剩「画像纠错 + 偶发垃圾」。

**文件层改动**（备份 `~/.hermes/backups/memory-tidy-20260917_1534/`，原文留档 ARCHIVE.md）：
- A 类硬错误 6 处纠正：视觉条（原生认图已通，旧「必报 400」失效）、主模型名（v4-flash→**v4.1-flash**）、9router 条模型名、LLM 渠道条 BiliSum 模型名（实查容器 env）、BiliSum 条、PRL 决策编号（D1–D11→**D23**，且与另一条自相矛盾 → 两条合并）
- B 类过期待办 4 处删除：macmini 服务托管待办（infra-index 实测已更新）、nasvm P1-2 loopback（方案文档「已完成」+ 实测全 loopback）、翻译项目（确认已停，压成一行）、bash 坑（已入 chisel-ops Pitfall 4）
- USER.md：memU 评测「验证中」→ 已出结论（36 查询，整体 memobase 优 0.769/0.730，auto 场景 memU 反超）；工程与委派 + 多 agent 分工两条合并
- STANDBY 高德条：与 ROUTES.md 重复 → 删 STANDBY 版（保留 ROUTES）
- ROUTES.md 视觉条同步纠正

**教训（本轮最有价值）**：
1. **「实验验证中」「待办」「疑似」这类标记会静默过期**——实测 6 条里 5 条早已完成/失效，纯凭标记读会一直当活跃待办留着。整理时必须**逐条去核实当前真实状态**（翻配置/实测/查文件 mtime），不能凭印象。
2. **换模型后要重测旧结论**：「vision_analyze 必报 400」只在主模型非多模态时成立，换 v4.1-flash 后原生直接通了。凡结论依赖「当前模型/配置」的，写清前提条件。
3. **同一文件内可能自相矛盾**（MEMORY 里 PRL 一条写 D1–D11、另一条写 D1–D23）——体检要做一致性交叉检查，不能只逐条看。

**清理后**：MEMORY 84.4%→80.9%；USER 90.1%→88.4%
**skill 沉淀（新增 5 处）**：hermes-bot-lifecycle（agent 编排三坑）、9router（配额峰谷表）、memobase-config（FR-019 锚点）、knowledge-ops（SOURCE_DIRS 现状）、vision-gateway（原生优先）

---

## 2026-09-11（定向清理：SOUL 诊断实验噪音）

**背景**：排查「SOUL 遵循变差」时小爱跑了 11 次一次性 CLI 会话做 A/B（同一个技术问题、换 SOUL 变体/换模型），这些会话都被 memory provider 当作真实对话写进了 buffer → 提取出测试派生事件。

**已执行**
- 删除 7 个纯测试事件（`5edbfc13` / `3da3c620` / `9890b0dc` / `6faf54a5` / `8e125ba2` / `7b1621c6` / `6c60c07c`）——内容均为「用户询问 prompt cache 压缩打断原因」「用户计划收口 loopback」等测试提问
- 单行删除 5 条混在真实事件里的测试 gist（`b63576d7` / `56631366` / `2c6c4a17` / `3e54973c` / `8e0eb307`）——保留同事件内真实内容（lazyssh/herdr 使用、Lightpanda 决策、145 commit 待办、SOUL 变差报告）
- 画像纠错 3 条（剔除测试派生分句，attributes 原样回传）：`d38e4efe` / `26e405c0` / `2bc12858`
- 备份：`backups/cleanup_20260911_soul-test-noise.json`
- 同步删除 Hermes 会话库里 11 个测试会话（`hermes sessions delete --yes`）

**第二次（更新后复验）**：Hermes 更新到 `fc3d60af09` 后，用新 SOUL 跑 1 次技术题实测会话 → 异步提取又产出 3 条 gist（`c7321213`/`5dbaae17`/`5b63bfa8`，同属 event `36a72b59`）。三条全为测试派生 → 全部单行删除，空壳 event `36a72b59` 一并删掉；备份追加在同一个 json 里。测试会话 `20260911_171630_9f5b26` 同步删除。

**清理后**：08:28 UTC 之后只剩 5 条 gist，全部为真实事件；孤儿检查 profiles/events/gists/summaries 均为 0

**教训**：一次性 CLI 会话（`hermes -z` / `chat -Q`）**不会**被 memory provider 排除，跑实验必须用独立 user 或事后定向清理。诊断类测试建议集中跑完立刻清，别等。

---

## 2026-09-10（第二轮体检）

**清理前**：画像 56 条 / 事件 620 条 / gist 2262 条 / 日概要 28 篇 / 用户 6 个

### 已执行

| 动作 | 目标 | 结果 |
|---|---|---|
| 修正画像 | `de8cee4b` basic_info/languages：`中文` → `母语中文，日常交流使用中文，技术术语常中英混杂` | ✅ errno=0 |
| 删除画像 | `20ff07cc` basic_info/family_member（内容为空字符串） | ✅ 画像 56→55 |
| 删除孤儿用户 | `f43c224e`（7/23 早期残留：4 画像 + 1 event + 3 gist，21 分钟后切到 `7e90d29c`） | ✅ 级联删除 |
| 删除空壳用户 | `6780d909`、`d26cb61f`、`a965e782`（0 数据） | ✅ |
| 残留检查 | profiles/events/gists/summaries/blobs 中 `user_id NOT IN users` | ✅ 全 0，无孤儿行 |

**备份**：`backups/cleanup_20260910.json`（孤儿用户全量 + 待修画像原值）

**剩余用户**：`7e90d29c`（沫沫）、`a1abd218`（克拉拉）

### 扫描结论（事件层）

2262 条 gist 中：极短(<15字符) 7 条、疑似系统噪音 7 条、重复组 5 组 → **整体很干净**，候选仅如下。

**A 类 纯垃圾（event 里只有这一条 gist）→ 建议删整个 event：**

| event | 日期 | 内容 |
|---|---|---|
| `1dcb0a65` | 08-29 | 无新信息。 |
| `6190e726` | 08-29 | 无新信息。 |
| `51bdaa1e` | 08-29 | 无新信息。 |
| `175ef09f` | 08-30 | 无新信息。 |
| `a2186376` | 08-27 | 无新增用户信息或日程安排。 |
| `19cf3acd` | 08-27 | 无新增用户信息或日程安排。 |

**B 类 残渣 gist（event 本身有价值，只删该条 gist，API 无 gist 删除端点，走 psql 单行删除）：**

| gist | 所属 event | 日期 | 内容 |
|---|---|---|---|
| `5555cdac` | `1aba1842`（6 条 gist） | 09-09 | `- 沫`（截断残渣） |
| `099a6785` | `6ac40fa9`（2 条 gist） | 07-29 | `-` |

**C 类 冗余静态陈述（画像层已覆盖，可选删）：**

| gist | 日期 | 内容 | 说明 |
|---|---|---|---|
| `8bce11d0` | 09-04 | 用户使用中文交流。 | 静态事实，languages 画像已覆盖 |
| `35a85bb8` | 09-07 | 用户使用中文交流。 | 同上 |
| `ea3a4dfe` | 07-23 | 沫沫讨厌 Qt GUI 崩溃类问题 | 同内容 2 份，保留新的 |
| `934ec5bd` / `ee9a66db` | 08-27 / 08-28 | 需要定义 1–3 个 Current Goal 解除根阻塞 | **建议保留**（时序状态，非静态事实） |

**执行结果（2026-09-10 沫沫确认后收尾）**：

| 项 | 动作 | 结果 |
|---|---|---|
| A 类 6 个空占位 event | API `DELETE /users/event/{uid}/{eid}` | ✅ 6/6 |
| B/C 类 5 条残渣 gist（`5555cdac`/`099a6785`/`8bce11d0`/`35a85bb8`/`240f8f58`） | psql 单行 DELETE | ✅ DELETE 5 |
| 概要悬空引用（8/27 ×2、8/29 ×3、8/30 ×1） | 从 `event_ids` 剔除，文本不动 | ✅ UPDATE 3 |

- C 类中「需要定义 1–3 个 Current Goal」（8/27、8/28）按计划**保留**（状态轨迹非静态事实）
- C 类 Qt GUI 重复：保留较晚的 `ea3a4dfe`(11:57)，删较早的 `240f8f58`(11:54)
- 备份：`backups/cleanup_20260910_events.json`（6 event + 5 gist 原值 + 28 条概要原值）
- 验证：目标行残留 0 ✅ / 悬空 event_ids 0 ✅ / 无主 gist 0 ✅ / healthcheck errno=0 ✅ / context 实测 errno=0（1320 字符正常注入）

**原则备忘**：event 是底层存档（`event_data.event_tip` 不因 gist 删除而改写）；gist / embedding / 概要 event_ids 是派生层，可清理。完全无信息的 event（内容即「无新信息」）整体删除。

**清理后**：画像 55 / 事件 615 / gist 2264 / 概要 28（事件数高于清理前是因为体检期间本会话在持续写入）


---

## 2026-08-28（第一轮体检）

- 删除 4 个噪音事件（`b4ad2838` / `0e1f5fd5` / `fb3bd6f0` / `a221485d`）
- 删除 2 条冗余画像（`81b288f2` / `c9308e2e`）
- 修正 3 条画像：密钥脱敏 `9e3c1841`、生日修正 `20ff07cc`、目标状态 `6c141227`
- 画像从 24 条精简至 22 条
- 结论：事件类记忆不会自动合并/去重，影像层会部分合并 → 保存纪律见 memobase-config skill
